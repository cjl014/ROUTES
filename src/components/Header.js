import React from 'react';

const styles = {
    background: 'linear-gradient(#626B7A, #19273D)',
    height: 30
}

export const Header = () => {
    return(
        <header style={styles} >

        </header>
    );
}